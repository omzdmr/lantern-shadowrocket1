#!name=Lantern v16 Single-Connection Test
#!desc=Routes only example.com through the selected proxy; everything else stays DIRECT. Diagnostic only.

[General]
bypass-system = true

[Rule]
DOMAIN,example.com,PROXY
DOMAIN,www.example.com,PROXY
FINAL,DIRECT
